{
  "name": "fit-journey-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "npm run start --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "start": "npm run start --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}